#include<iostream>
#include<algorithm>
using namespace std;
int main(){
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int L, C;
    char cons[], vowel[], tmp;
    cin >> L >> C;
    for(int i = 0; i < C; i++){
        cin > tmp;
    }
    return 0;
}
